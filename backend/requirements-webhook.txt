# Webhook Lambda - minimal dependencies
# Only needs boto3 (included in Lambda runtime) and psycopg2 for DB
#
# M-11: same pinning policy as requirements-api.txt
psycopg2-binary==2.9.10

# Bank statement processing
pdfplumber==0.11.4
pdfminer.six==20240706
PyYAML==6.0.2
