# TRW Backend Dependencies
# Python CSV processing engine for crypto tax formatting

# Core dependencies
pandas>=2.0.0
chardet>=5.0.0

# Database (Neon PostgreSQL)
psycopg2-binary>=2.9.0

# AWS SDK (Lambda uses bundled boto3, but useful for local dev)
boto3>=1.26.0

# AI Providers
anthropic>=0.40.0
google-generativeai>=0.8.6

# Optional: For testing
pytest>=7.0.0
pytest-cov>=4.0.0
