# API Lambda - dependencies for CSV processing + bank statement parsing
# Same as processor but without AI SDK (API doesn't run AI insights)
#
# M-11: All deps pinned. Loose pins on pdfplumber permitted very old
# pdfminer.six versions with known XXE / ReDoS CVEs. See SECURITY_AUDIT.md §M-11.
numpy==1.26.4
pandas==2.1.4
chardet==5.2.0
psycopg2-binary==2.9.10

# Bank statement processing
# pdfplumber hard-pins its pdfminer.six version — bump both together.
pdfplumber==0.11.9
pdfminer.six==20251230
PyYAML==6.0.2
