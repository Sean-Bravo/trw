# Playwright test credentials (M-19)
#
# Copy to .env.test.local (gitignored) and fill in.
# These are loaded by playwright.config.ts at test time.
#
# NEVER put production secrets in this file — it's strictly for test
# fixtures. NEVER use the same email as a real user account.
#
# See SECURITY_AUDIT.md §M-19

E2E_USER_EMAIL=playwright-test@example.com
E2E_USER_PASSWORD=
