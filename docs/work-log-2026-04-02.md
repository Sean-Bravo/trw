# Work Log — 2026-04-02

## Summary

Published the TaxFormatter Python SDK (`taxformatter-sdk`) to PyPI and set up automated publishing via GitHub Actions.

## Completed

### 1. Favicon Dark Mode Fix (carried over from session)
- Switched `app/layout.tsx` from `favicon.ico` to `icon.svg` to eliminate white border in dark browser chrome
- The SVG has a solid blue (`#3b82f6`) rounded-rect background that renders cleanly in all themes
- **Commit:** `2fe5f7e`

### 2. GitHub Actions Workflow for PyPI Publishing
- Created `.github/workflows/publish-pypi.yml`
- Triggers on version tags (`v*.*.*`)
- Runs Python SDK tests with pytest, then builds and publishes using trusted publisher (OIDC, no API token)
- **Commit:** `f5574bc`

### 3. PyPI Package Name Resolution
- `taxformatter` was rejected by PyPI as an invalid project name
- Renamed package to `taxformatter-sdk` in `pyproject.toml` and `README.md`
- Import remains `from taxformatter import TaxFormatter` (unchanged)
- **Commit:** `5f79b1e`

### 4. Hatchling Build Fix
- First publish attempt failed — hatchling couldn't find a `taxformatter_sdk/` source directory
- Added `[tool.hatch.build.targets.wheel]` config to map the `taxformatter/` directory
- **Commit:** `ed69d9f`

### 5. Published to PyPI
- Set up trusted publisher on PyPI (pending publisher for `taxformatter-sdk`)
- Created `pypi` environment in GitHub repo settings
- Tagged `v0.1.0` and pushed — workflow ran successfully
- Verified: `pip install taxformatter-sdk` works, import succeeds
- **Registry:** https://pypi.org/project/taxformatter-sdk/

## Known Issues

- CI workflow (`ci.yml`) failing on Lint & Unit Tests — pre-existing, unrelated to today's changes. Jest coverage thresholds not met globally.
